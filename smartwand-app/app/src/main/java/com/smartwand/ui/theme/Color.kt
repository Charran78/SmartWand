package com.smartwand.ui.theme

import androidx.compose.ui.graphics.Color

// Primary brand colors - Accessibility focused (high contrast)
val WandBlue = Color(0xFF1976D2)          // Primary action color
val WandBlueDark = Color(0xFF0D47A1)      // Primary dark variant
val WandBlueLight = Color(0xFF64B5F6)     // Primary light variant

// Emergency colors - Maximum visibility
val EmergencyRed = Color(0xFFD32F2F)      // Emergency button/alerts
val EmergencyRedDark = Color(0xFFB71C1C)  // Emergency pressed state
val SafeGreen = Color(0xFF388E3C)         // Success/safe states

// Neutral colors
val SurfaceLight = Color(0xFFFFFBFE)
val SurfaceDark = Color(0xFF1C1B1F)
val OnSurfaceLight = Color(0xFF1C1B1F)
val OnSurfaceDark = Color(0xFFE6E1E5)

// Accent colors
val AccentAmber = Color(0xFFFFA000)       // Warnings, battery low
val AccentPurple = Color(0xFF7C4DFF)      // Genie/AI features

// Background gradients
val GradientStart = Color(0xFF1565C0)
val GradientEnd = Color(0xFF0D47A1)
